# BorrowEase Server Environment Example (NO SECRETS)
# Copy to .env and fill in real values. Never commit real secrets.

# --- MongoDB ---
MONGO_URI=mongodb://localhost:27017/borrowease

# --- Email Queue ---
EMAIL_PROVIDER=smtp
EMAIL_WORKER_INTERVAL_MS=5000
EMAIL_WORKER_BATCH=10
# EMAIL_WORKER_DEBUG=false
# EMAIL_DEBUG=false

# --- SMTP (Gmail early-stage) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=youraddress@gmail.com
SMTP_PASS=your_gmail_app_password_16chars
MAIL_FROM="BorrowEase Support <youraddress@gmail.com>"

# --- (Future) SendGrid ---
# EMAIL_PROVIDER=sendgrid
# SENDGRID_API_KEY=SG.xxxxxx
# SENDGRID_FROM=support@yourdomain.com
# SENDGRID_SMTP_FALLBACK=false

# --- Cloudinary ---
# Server-side (keep secret)
CLOUDINARY_CLOUD_NAME=replace_me
CLOUDINARY_API_KEY=replace_me
CLOUDINARY_API_SECRET=replace_me

# Client-side (public)
VITE_CLOUDINARY_CLOUD_NAME=replace_me
VITE_CLOUDINARY_API_KEY=replace_me

# --- Other feature flags / placeholders ---
# MAX_CONCURRENT_EMAILS=5
# EMAIL_MAX_ATTEMPTS=5

# --- Firebase Admin (choose ONE approach) ---
# Option A: Base64-encoded JSON of service account (recommended)
# FIREBASE_SERVICE_ACCOUNT_BASE64=ew0KICAicHJvamVjdF9pZCI6ICJ... (base64)

# Option B: Discrete fields
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-abc123@your-project-id.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEv...\n-----END PRIVATE KEY-----\n"

# --- Payments (Razorpay) ---
# RAZORPAY_KEY_ID=rzp_test_xxx
# RAZORPAY_KEY_SECRET=xyz
